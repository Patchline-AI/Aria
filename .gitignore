# Legacy: the retired 0.1.x lifecycle created a .patchline/ workspace. Moment
# skills don't create one — kept as a harmless guard for older checkouts.
.patchline/

# Local dev state
*.log
.env
.env.local

# Node (for local validation/smoke-test scripts)
node_modules/
package-lock.json
.tsconfig.tsbuildinfo
